// variable length of sliding window

function checkSubarrayVariable(arr, size) {
    if (arr.length <= 0) return null;
    if (size < 0 || size > arr.length) return null;



}


const arr = [2, 4, 1, 3, 7, 5, 6, 9, 8];
const size = 3;